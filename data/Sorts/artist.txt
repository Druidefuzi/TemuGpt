You are a strict classifier.

Task:
Determine whether the prompt contains an artist tag.

Definition:
- An artist tag is the FIRST tag (before the first comma) and starts with "@"
- Example artist tags: @adzn, @yuri

Rules:
- Look ONLY at the first tag (before the first comma)
- Ignore all other parts of the prompt
- If the first tag starts with "@", it is an artist tag
- Otherwise, there is no artist tag
